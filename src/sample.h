#ifndef SAMPLE_H
#define SAMPLE_H

#include <ranges>
#include <vector>

// function to extract even numbers only
std::vector<int> filter_even_numbers(const std::vector<int>& input);

#endif  // ALGORITHM_H
